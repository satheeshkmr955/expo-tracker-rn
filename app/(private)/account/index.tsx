import Animated from "react-native-reanimated";

const AccountScreen = () => {
  return (
    <Animated.View>
      <Animated.Text>Account Screen</Animated.Text>
    </Animated.View>
  );
};

export default AccountScreen;
